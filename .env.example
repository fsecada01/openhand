# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=
# Intake extraction (feeds the deterministic engine — keep strong).
ANTHROPIC_MODEL=claude-opus-4-8
# Explanation pass (constrained rewording — a lighter, cheaper model
# is fine; Haiku 4.5 works well here).
ANTHROPIC_EXPLAIN_MODEL=claude-haiku-4-5
DATABASE_URL=sqlite:///./openhand.db
# Required by sqlmodel-crud-utilities at import time.
SQL_DIALECT=sqlite
# Privacy: keep false outside local debugging.
STORE_NARRATIVES=false
RATE_LIMIT_SCREEN=10/minute
# Optional: enables the "other resources to check" web-search
# fallback for programs that screen as ineligible/undetermined.
TAVILY_API_KEY=
